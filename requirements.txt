# --------------------------------------------------------------------------
# UniverSat — Python dependencies.
#
# The torch.hub release path only needs `torch` (and numpy, which ships with
# it). Everything else here is for the training / evaluation pipeline.
# --------------------------------------------------------------------------

# --- core ML stack ---
torch>=2.2.0
torchvision>=0.17.0
lightning>=2.2.0          # Lightning 2.x — replaces standalone pytorch_lightning
lightning-bolts           # pl_bolts.optimizers.LinearWarmupCosineAnnealingLR (configs/model/SemSeg.yaml)
torchmetrics>=0.11.4

# --- training-time tensor ops ---
einops                    # used by Predictor (SSL training only)

# --- hydra ---
hydra-core==1.3.2
hydra-colorlog==1.2.0
# omegaconf is pulled in transitively by hydra-core

# --- logging ---
wandb
rich
tqdm

# --- numerical / scientific ---
numpy
scipy
pandas
matplotlib
scikit-learn

# --- EO data I/O ---
rasterio
tifffile
opencv-python
h5py
pyarrow
geopandas==0.14.4
tfrecord                  # Planted dataset reads TFRecord files
datasets                  # HuggingFace datasets — EarthView (NEON) loader
Pillow

# --- config / utilities ---
pyrootutils               # resolves project root via .project-root
PyYAML
timm
stopit                    # timeout decorator used by LP eval

# --- testing ---
pytest

# --- optional, dev-only ---
# pre-commit              # lint hooks
